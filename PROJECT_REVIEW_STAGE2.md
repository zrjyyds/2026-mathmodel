# 2026 数学建模竞赛 D 题项目复审报告（Stage 2）

审查日期：2026-09-23  
审查基准：当前 `HEAD=1c6bf66`，与 `upstream/main` 一致  
审查范围：当前 Git 树、提交历史、LaTeX 源文件、已编译 PDF、Word 方案报告及其关联证据  
审查目标：评价“山区洪涝灾害下无人机运输与通信协同优化”的数学模型、求解方案和结果可信度  

> 本次没有修改任何代码、数据、论文源文件或构建产物，只新增本报告。

## 1. 复审结论

新版本相对于 Stage 1 的实质性进步，是将问题四写成了独立的数学模型：以跨服务区运输架次构造连接图，得到 13 个不可拆组件，完整枚举 K=2、K=3 的无标签分区，并定义了组内峰值资源需求和库存缺口。论文结构也从旧的“敏感性分析 + 模型评价”顺序，调整为“问题四 + 敏感性分析 + 模型评价”。

但本次同步没有补入求解源码、原始数据、求解器输出、路线表或验证日志。因此，18 架次、20 架次、9980 s、通信中继能耗 1.797437 kWh 以及 K=2/K=3 缺口等数值，仍只能视为论文中的结果陈述，不能视为当前仓库可独立复核的计算结果。

当前方案可以评价为：**数学建模叙事已经覆盖四问，问题四的形式化明显增强；但结果证据链、物理一致性和问题二/三的全局求解能力仍不足以支撑“已验证最优方案”的表述。**

## 2. 当前 HEAD 与版本变化

### 2.1 Git 状态

```text
HEAD              1c6bf66  Delete paper/sections/8_sensitivity.tex
upstream/main     1c6bf66
origin/main       dd8651f（本地 main 比 origin/main 超前 9 个提交）
工作区            仅有未跟踪的 PROJECT_REVIEW_STAGE1.md
```

`PROJECT_REVIEW_STAGE1.md` 是上一阶段产生的工作区文件，不属于当前 HEAD。本报告不把它当作项目实现内容。

### 2.2 新增、删除和修改

相对于上一阶段所依据的版本，发生了以下变化：

| 变化 | 文件 | 影响 |
|---|---|---|
| 新增 | `paper/sections/8_problem4.tex` | 问题四首次拥有独立的变量、约束、枚举规模、分区结果和资源缺口解释 |
| 新增 | `paper/sections/9_sensitivity.tex` | 敏感性分析改为第 9 节，保留返航余量、通信容量、地形净空和采样步长 |
| 新增 | `paper/sections/10_evaluation.tex` | 模型优点、不足、推广和结论改为第 10 节 |
| 删除 | `paper/sections/8_sensitivity.tex` | 旧的第 8 节敏感性分析不再存在 |
| 删除 | `paper/sections/9_evaluation.tex` | 旧的第 9 节模型评价不再存在 |
| 较早修改 | `paper/main.tex` | 主文件已改为输入 `8_problem4`、`9_sensitivity`、`10_evaluation`，并引入 `amssymb` |
| 较早更新 | `paper/main.pdf` | 当前 PDF 为 16 页，包含问题四新章节 |

没有发现新增或删除的 Python、JavaScript、Excel、CSV、DEM、求解器配置、结果表或实验脚本。

## 3. 哪些 Stage 1 结论已失效

以下结论需要更新：

1. **目录结构结论失效**：旧报告列出的 `8_sensitivity.tex` 和 `9_evaluation.tex` 已被删除；当前应改为 `8_problem4.tex`、`9_sensitivity.tex`、`10_evaluation.tex`。
2. **“问题四只有总结性文字”的结论失效**：当前问题四已经给出组件分区变量、非空约束、峰值并发公式、缺口公式、分区数量和具体分组结果。
3. **“论文只有旧四问结构”的结论失效**：当前论文正文已经明确形成问题一至问题四、敏感性分析、模型评价的十节结构。
4. **旧 PDF 页数结论失效**：当前 `paper/main.pdf` 为 16 页，并包含问题四内容；不能继续引用旧版 14 页作为当前状态。

以下结论仍然有效：

1. 仓库中没有可执行的数学求解源码；附录只是伪代码，并引用不存在的 `model_utils.py`、`problem1.py`、`problem3.py`、`problem4.py`、`verify_all.py` 和 `repair_problem2_schedule.py`。
2. 没有原始 Excel、DEM、CSV 或结果快照，无法从当前 checkout 重算任何四问结果。
3. Word 方案报告第 13 节仍明确写着“尚未运行优化程序”，并要求后续才把结果写入 `results/`。这与论文正文已经写入具体数值存在证据冲突。
4. 问题一返程载荷、问题二候选列回溯的完备性、问题三中继容量和 30 s 离散采样等数学风险没有被新提交消除。

## 4. 当前论文结构

`paper/main.tex` 当前的实际章节链为：

```text
封面与摘要
目录
1  问题重述
2  问题分析
3  模型假设
4  符号说明
5  问题一的模型建立与求解
6  问题二的模型建立与求解
7  问题三的模型建立与求解
8  问题四的模型建立与求解
9  敏感性分析
10 模型评价与推广
参考文献
附录 A 核心代码（实际为伪代码）
```

当前 `paper/main.pdf` 已呈现这套新结构；但是 `paper/main.log`、`paper/main.aux` 和 `paper/main.toc` 仍记录旧的 `8_sensitivity`、`9_evaluation` 和旧页数，说明构建辅助文件没有随源文件同步更新。PDF 可以作为排版成品，不能作为求解结果的来源证明。

## 5. 当前数学模型设计

### 5.1 公共物理层

方案统一使用服务区、调度中心、DEM、航段距离、巡航高度、飞行时间、载荷相关水平能耗和爬升能耗。运输机沿地面投影直线飞行，巡航高度取 DEM 最高点上方 50 m 与作业高度的较大值；通信使用双向链路预算和 DEM 遮挡判断。

这套口径使四问能够共享参数，优点是递进关系清楚。局限是：直线投影、固定净空、静态 DEM 和 30 s 采样均是简化，不能自动代表真实山地航迹或连续通信。

### 5.2 问题一：安全载荷与组批

模型以质量、体积和返航安全余量约束可行货箱子集，再用服务区内位掩码 DP 做集合划分。这个方法与小规模不可拆货箱组批相符，若每个服务区货箱数不大，具有得到定义域内最优组批的潜力。

需要保留的关键审查意见是：论文写作

```text
E_out(m_p) + E_return(m_p) <= (1-rho) E_b
```

但货物投放后返程通常应使用空载或剩余载荷 `E_return(0)`，而不是继续使用 `m_p`。除非题目明确规定返程仍携带同等载荷，否则 18 架次和 57.937430 kWh 不能在该口径未澄清前视为可信。水平能耗公式还需要与题面给出的航程/功耗关系保持同一物理定义，不能把等效航程直接当成实测能量模型。

### 5.3 问题二：异构多点运输调度

候选列记录路线、机型、货箱集合、持续时间、能耗和逐箱交付偏移量，再用硬时限、运输机占用区间和电池充电区间进行回溯排程。允许双服务区架次，方向是正确的。

但当前方法应被称为“候选列 + 资源回溯启发式”，不能直接等同于列生成或 VRP 全局求解：

- 没有给出定价子问题、完整候选列生成规则或候选列覆盖充分性证明；
- 回溯只说明尝试资源冲突诱导的边界时刻，不能证明对所有路线、排序、机型和起飞时刻完备；
- 没有下界、最优性 gap、基准算法或多次运行稳定性；
- 论文的 20 架次、9980 s 和 61.207790 kWh 因而最多是一个待验证可行解陈述。

与主流 VRP/无人机配送研究相比，模型约束较丰富，但求解证据弱于 MILP/CP-SAT 精确模型、分支定界、ALNS 或大邻域修复方法。

### 5.4 问题三：通信约束与中继

新版本没有改变问题三的基本结构：固定问题二运输轨迹，按 30 s 采样直连状态，对直连缺口枚举航段比例 `{0.35, 0.50, 0.65}` 和多个高度，再用分支定界选择中继关系。

其合理性在于把 DEM 遮挡、双向链路和中继悬停能耗纳入运输问题；但创新性仍主要来自题目场景的组合，而不是新的通信优化方法。主要风险是：

1. 中继位置只取少量比例点，不能证明覆盖全体可行位置；
2. 30 s 采样不能严格保证全程连续通信，可能漏掉短时中断；
3. 主方案允许通信列并行保障，而严格单实体中继情景在固定问题二时序下不可行；
4. 问题三冻结问题二时序，实际没有完成运输起飞时刻与中继资源的联合调度；
5. 当前库存只有 2 架中继机，而问题四在冻结时序下计算出 K=2 需要 4 架、缺口 2 架。因此问题三应表述为“在并行通信抽象下的关系方案”，不能直接表述为现有中继库存下的完整可执行方案。

### 5.5 问题四：任务分区与资源配置

这是新版本最有价值的新增部分。模型将跨服务区架次转为连接边，得到 13 个不可拆组件；对组件进行无标签非空分区，枚举规模为

```text
S(13,2)=4095，S(13,3)=261625。
```

并定义固定任务时序下的组内峰值资源需求：

```text
N_hr^min = max_t sum_j 1(t 属于任务 j 对资源 r 的占用区间)
Delta_r = max(0, sum_h N_hr^min - S_r)
```

这比旧版本只有结论性描述更完整，也明确区分了“峰值并发下界”和“实际使用的资源 ID 数”。不过它的数学语义必须写清：这是**冻结问题二、问题三时序后的资源核算**，不是允许每个分区重新排程时的全局最小资源配置。若组间可以错峰，冻结时序得到的缺口是保守配置需求，而非一般意义上的最优缺口。

当前报告的 K=2 分区缺口 3、K=3 分区缺口 4，在给定组件和任务区间真实存在的前提下，枚举逻辑是可信的；但组件、区间和资源数均来自尚未入库的上游结果，当前仓库无法复核。

## 6. 当前结果可信度

| 结果 | 论文陈述 | 当前证据等级 | 判断 |
|---|---:|---|---|
| Q1 架次/能耗 | 18 / 57.937430 kWh | 文档陈述 | 返程载荷口径未消除，不能复核 |
| Q2 架次/完成时刻/能耗 | 20 / 9980 s / 61.207790 kWh | 文档陈述 | 回溯非全局求解，且无路线表、日志或输入 |
| Q3 直连/中继/中继能耗 | 14 / 6 / 1.797437 kWh | 文档陈述 | 依赖并行通信假设，严格中继资源情景不可行 |
| Q4 组件与缺口 | 13 个组件；缺口 3/4 | 文档陈述 | 形式化完整，但依赖冻结的 Q2/Q3 结果 |
| 独立回放全通过 | 论文摘要与正文声称 | 无验证器 | 附录引用的 `verify_all.py` 不存在 |

Word 方案报告明确表示尚未运行优化程序，因此当前最稳妥的论文措辞应是“模型计算得到的待复核结果”或“方案预估值”，不能写成已由程序独立回放确认的事实。

## 7. 数学建模方面的优势

1. 四问递进关系明确：运输能力 → 多点调度 → 通信保障 → 任务分区。
2. 货箱不可拆、体积、载荷、返航余量、硬时限、共享电池和通信链路等题目关键约束均被识别。
3. 候选列和资源区间适合把组合问题与排程问题连接起来，问题四的连接分量建模也有清晰工程解释。
4. 新问题四给出了完整分区数量和资源缺口指标，比旧版本更接近可审查的数学模型。
5. 论文主动承认 Q2/Q3 非全局最优、通信容量缺参和离散采样局限，结论边界意识较好。

## 8. 主要不足与竞争风险

1. **最主要风险是结果不可复核**：没有原始数据、代码、路线、资源时间线、链路状态表或求解日志。
2. **Q1 物理公式可能直接改变结论**：返程载荷写法需要先统一。
3. **Q2 求解方法偏弱**：候选列与回溯能找到可行解，但没有证明候选集完备或解质量优于其他方案。
4. **Q3 不是严格联合优化**：通信是在冻结运输排程后补充的，且主结果依赖未给出容量的并行通信抽象。
5. **Q4 的“最小”容易被误读**：它是固定时序下的峰值资源最小值，不是允许重排后的全局最小库存。
6. **论文证据链不一致**：正文称独立回放通过，附录只有伪代码，Word 方案却称尚未运行程序；构建日志和辅助文件也与当前章节不同步。
7. **容易被其他队伍超过的地方**：其他队伍只要提供真实可复现的数据处理、标准求解器或 ALNS 对比、通信容量可行方案、路线甘特图和敏感性原始表，就能在可信度和结果说服力上超过当前方案，即使其模型形式不如当前方案复杂。

## 9. Stage 2 后的最终判断

| 维度 | 当前判断 |
|---|---|
| 四问覆盖 | 已覆盖，问题四形式化显著增强 |
| 数学结构 | 中等偏上，递进关系和约束识别较完整 |
| 求解有效性 | Q1 方法有潜力；Q2/Q3 只能证明方案级启发式思路，不能证明全局最优 |
| 物理可信度 | 中等偏低，返程载荷、通信容量和采样连续性需先澄清 |
| 结果可信度 | 低，当前仓库不能重算或独立验证 |
| 竞赛竞争力 | 作为模型草案有竞争力；作为最终论文，证据链不足是决定性短板 |

## 10. 提交前必须完成的数学修正顺序

1. 先固定输入数据、单位、坐标系、DEM 取样和返程载荷口径，重新计算 Q1。
2. 将 Q2 明确标注为可行启发式，给出候选列规模、搜索停止条件、基准解和可行性/下界信息。
3. 将 Q3 改成具有明确中继实体、位置占用、服务区间和通信容量假设的可行模型；至少同时报告并行抽象和严格单实体情景。
4. 对通信状态增加端点、事件点和更细步长收敛检查，避免把 30 s 离散检查称为严格连续保证。
5. 将 Q4 的结论明确限定为“冻结运输与通信时序下的峰值资源配置”，并说明若允许分区重新排程，结论会改变。
6. 只在所有数值由同一套输入和求解记录产生后，保留“独立回放通过”等强结论；否则应删除或降级措辞。

**复审结论：新版本完成了问题四的实质性补强，但没有改变项目最核心的状态——当前仍是一个数学建模论文方案和排版成品，而不是已经被数据、程序和验证证据支撑的最终竞赛解。**
