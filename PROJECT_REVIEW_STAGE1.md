# PROJECT_REVIEW_STAGE1

> 审查日期：2026-09-23（Asia/Shanghai）  
> 审查范围：当前 Git `HEAD`、工作区文件、LaTeX 源码、已编译 PDF、编译日志和 Git 历史。  
> 写入约束：本次只新增本报告；未修改任何已有代码、文档、数据或构建产物。

## 0. 结论摘要

当前仓库不是一个可运行的数学建模 Agent，而是一个“数学建模方案/论文交付物 + 预研设计报告”的仓库：

- **已存在**：题目拆解、数学公式、约束假设、四问的算法蓝图、LaTeX 论文源、已编译 PDF 和一份 Word 导出的方案草稿。
- **不存在**：Python/JS/其他业务源码、Agent 入口、Planner、Executor、Memory、Skill 注册/加载机制、Prompt、工具适配器、数据附件、求解器依赖、测试、CI、实验脚本、结果快照和自动论文生成器。
- **唯一可验证的运行链**：在具备 XeLaTeX/TeX Live 和相应字体的环境中，编译 `paper/main.tex` 生成论文 PDF。当前 PowerShell 中未发现 `xelatex` 可执行命令；仓库内的 `paper/main.log` 证明此前曾在另一套 Windows TeX Live 2024 环境编译成功。
- **四问结果的可信度边界**：论文正文写有 18/20 架次、能耗、通信和分区缺口等数值，但生成这些数值所需的代码、原始数据、运行日志、结果表和验证器均未入库；根目录 Word 方案报告末章反而明确写着“尚未运行优化程序”。因此本报告将这些数字标记为“文档声称/不可在当前 checkout 复现”，不将其当作已验证的程序输出。

证据等级：

- **[实现]**：当前仓库中可执行或可直接编译的文件。
- **[文档]**：论文/方案中描述的设计或伪代码，不能自动推断为实现。
- **[缺失]**：通过文件清单、全文检索和 Git 树确认不存在。

## 1. 项目结构

### 1.1 项目结构树

```text
D:\2026-mathmodel\
├── README.md
├── ANALYSIS_MODELING_REPORT_word_render.pdf
└── paper/
    ├── main.tex
    ├── references.tex
    ├── sections/
    │   ├── 1_restatement.tex
    │   ├── 2_analysis.tex
    │   ├── 3_assumptions.tex
    │   ├── 4_symbols.tex
    │   ├── 5_problem1.tex
    │   ├── 6_problem2.tex
    │   ├── 7_problem3.tex
    │   ├── 8_sensitivity.tex
    │   ├── 9_evaluation.tex
    │   └── A_code.tex
    ├── logo.pdf
    ├── title.pdf
    ├── title-img-01.png
    ├── main.pdf
    ├── main.aux
    ├── main.toc
    ├── main.log
    └── missfont.log
```

`.git/` 是版本控制元数据，不属于应用目录。除上述目录外，没有 `src/`、`app/`、`agent/`、`skills/`、`prompts/`、`data/`、`scripts/`、`tests/`、`results/`、`.github/` 或 workflow 目录。

### 1.2 一级目录和二级关键目录

| 路径 | 类型 | 作用和当前状态 |
|---|---|---|
| `paper/` | 一级目录 | 论文 LaTeX 源、封面素材和编译产物；是当前唯一的工程目录。 |
| `paper/sections/` | 二级目录 | 论文各章节与附录；包含模型叙述和一段伪代码，不包含真正的 Python 模块。 |
| `.git/` | VCS 元数据 | Git 对象、分支和远端配置，不提供运行能力。 |

仓库共有 3 个提交：初始 README、一次论文/PDF 文件上传、一次 README 更新。`git ls-tree -r HEAD` 与工作区递归扫描结果一致，没有被忽略或悬空的源码对象；`.gitignore` 也不存在。

### 1.3 文件/文档职责

| 文件 | 职责 | 证据/备注 |
|---|---|---|
| `README.md` | 项目进度备注 | 仅 3 行，没有安装、运行、数据或 Agent 说明。 |
| `ANALYSIS_MODELING_REPORT_word_render.pdf` | 题目分析与后续实现计划 | 16 页，Word 导出；列出了预期输入、算法、技术栈和阶段边界，但没有随附源码/数据。嵌入的 113 个对象均为 `equation-*.xml` 公式，不是代码或数据。 |
| `paper/main.tex` | LaTeX 主入口和模板 | `ctexart`、页面/字体/目录/表格/代码排版；通过 `\input` 引入章节。 |
| `paper/sections/1_restatement.tex` | 题目背景和四问重述 | 描述运输、调度、中继、分区的递进关系。 |
| `paper/sections/2_analysis.tex` | 统一物理层、候选列、资源区间和通信/分区思路 | 设计说明，无函数实现。 |
| `paper/sections/3_assumptions.tex` | 8 项模型假设 | 含直线飞行、DEM 净空、返航余量、充电、双向链路、并行中继容量等假设。 |
| `paper/sections/4_symbols.tex` | 符号表 | 质量、能耗、时间、链路、分区资源等符号。 |
| `paper/sections/5_problem1.tex` | 问题一模型 | 安全载荷公式、货箱候选子集、位掩码 DP、论文声称的结果。 |
| `paper/sections/6_problem2.tex` | 问题二模型 | 候选运输列、硬时限、无人机/电池时间区间、回溯排程、结果叙述。 |
| `paper/sections/7_problem3.tex` | 问题三模型 | DEM/链路预算、30 s 采样、中继候选、分支定界、容量敏感性。 |
| `paper/sections/8_sensitivity.tex` | 敏感性文字 | 返航余量、通信容量、DEM 净空/步长、K=2/3 分区。无扫描脚本或结果文件。 |
| `paper/sections/9_evaluation.tex` | 模型评价和推广 | 论文自述优点、不足和后续方向；明确 Q2/Q3 不是已证明的全局最优。 |
| `paper/sections/A_code.tex` | 核心流程伪代码 | 引用多个不存在的 `.py` 文件；只有函数名、参数和 `assert`，没有函数体。 |
| `paper/references.tex` | 4 条参考文献 | 竞赛题、动态规划、凸优化、无线通信。 |
| `paper/main.pdf` | 已编译论文 | 14 页，XeTeX 产物；没有结果地图、曲线或甘特图。 |
| `paper/*.aux/*.toc/*.log/missfont.log` | LaTeX 构建中间文件和日志 | 已提交到 Git，属于生成物，不是求解结果。 |
| `paper/logo.pdf`、`title.pdf`、`title-img-01.png` | 封面资源 | `main.tex` 实际引用 `logo.pdf` 与 `title.pdf`；PNG 未在主文件中被引用。 |

PDF 审查结果：根目录 Word PDF 为 16 页、创建者为 Microsoft Word；`paper/main.pdf` 为 14 页、创建者为 XeTeX，且无嵌入附件。`paper/main.log` 最后一行显示 `Output written on main.pdf (14 pages)`，但同时记录了 SimSun/SimHei/KaiTi/FangSong 和 Menlo 缺失或字体形状替代警告。

## 2. 代码架构

### 2.1 入口和模块扫描结论

| 目标组件 | 扫描结果 | 结论 |
|---|---|---|
| Python 入口 | 没有 `.py`、`__main__`、CLI、脚本或包配置 | [缺失] |
| JavaScript/TypeScript 入口 | 没有 `.js/.ts/package.json` | [缺失] |
| Agent 入口 | 没有 `agent/`、服务端、API 路由、启动器 | [缺失] |
| Skill 模块 | 没有 `skills/`、`SKILL.md`、注册表或加载器 | [缺失] |
| Prompt 文件 | 没有 prompt/template/yaml/json 配置 | [缺失] |
| 数据处理模块 | 没有 Excel/CSV/DEM 文件或读取代码 | [缺失] |
| 模型/求解模块 | 没有求解器导入、模型类或结果落盘代码 | [缺失] |
| 论文生成模块 | 有 LaTeX 源和已生成 PDF；无自动回填/生成脚本 | [实现：静态]，[缺失：自动化] |
| 工具调用模块 | 没有 LLM function schema、subprocess wrapper、工具注册或外部 API 适配器 | [缺失] |
| 测试/CI | 没有 `tests/`、pytest/unittest、GitHub Actions、Makefile | [缺失] |

### 2.2 实际可验证的调用关系

当前唯一真实的调用链是文档构建链：

```text
xelatex paper/main.tex
  └─ main.tex
      ├─ \includegraphics{logo.pdf/title.pdf}
      ├─ \input{sections/1_restatement ... 9_evaluation}
      ├─ \input{references.tex}
      └─ \input{sections/A_code.tex}
          └─ 生成 paper/main.pdf、.aux、.toc、.log
```

这条链只排版文本和公式，不读取赛题数据，也不调用数学求解器。

### 2.3 论文附录中的“伪调用图”（不是可执行调用图）

`paper/sections/A_code.tex:3-28` 给出如下顺序式接口：

```text
load_data()
  -> solve_zone_partition(...)                 # Q1
  -> build_transport_columns(...)
  -> repair_schedule(..., deadline_first=True,
                     include_battery_charge=True) # Q2
  -> build_relay_columns(...)
  -> solve_shared_relay(..., include_component_charge=True) # Q3
  -> enumerate_partitions(..., k_values=(2, 3))             # Q4
  -> verify_all(...).all_hard_checks_pass
```

附录声称实现文件为 `model_utils.py`、`problem1.py`、`problem3.py`、`problem4.py`、`verify_all.py` 和 `repair_problem2_schedule.py`，但这些路径在当前 Git 树中全部不存在。伪代码没有 `import`、函数体、类、输入 schema、异常处理、日志、结果保存或命令行入口，因此只能作为设计接口记录。

### 2.4 计划中的数据/模型/输出流

根目录 Word 方案报告第 3--6、12--13 节描述的**计划流**为：

```text
Excel / DEM / GIS CSV
  -> pandas/openpyxl/rasterio/geopandas 标准化
  -> 公共航段、DEM、时间、能耗、链路计算层
  -> Q1 安全载荷与组批
  -> Q2 多点候选列与资源排程
  -> Q3 视线/FSPL/中继联合优化
  -> Q4 不可拆组件分区与库存缺口
  -> 独立硬约束回放
  -> xlsx / JSON / Markdown + matplotlib/GeoPandas 图表
  -> LaTeX/PDF 论文
```

上述每个箭头（除最后的静态 LaTeX 排版外）在当前仓库均没有对应实现或输入文件。

## 3. Agent 能力分析

| 能力 | 当前是否存在 | 证据与判断 |
|---|---:|---|
| Planner | 否 | Q1→Q4 是固定数学流程，不是可接收用户目标、拆解任务、重规划的 Agent Planner；没有计划状态、任务 DAG 或 planner 类。 |
| Executor | 否 | 没有执行循环、任务队列、重试、超时、状态转移或命令执行器；附录函数未定义。 |
| Memory | 否 | 没有会话状态、持久化 checkpoint、向量库、结果缓存、实验数据库或记忆读写接口。 |
| Skill 系统 | 否 | 没有 Skill 目录、manifest、路由、版本或动态加载机制。 |
| Tool calling | 否 | 没有模型函数调用协议、工具 schema、数据读写适配器、求解器适配器或浏览/绘图工具封装。LaTeX 的 `\input` 不是 Agent tool calling。 |
| 自动实验 | 否 | 敏感性章节只写扫描范围；没有 runner、配置矩阵、随机种子、并行执行、结果缓存或实验日志。 |
| Prompt 设计 | 否 | 全文无系统提示词、角色提示词、模板、结构化输出约束或提示版本管理。 |
| Workflow 编排 | 否 | 没有 YAML/JSON workflow、DAG 引擎、调度器、CI 或可恢复工作流。 |

因此，“数学建模 Agent”目前只是用户对仓库目标的称呼；从仓库事实看，Agent 层尚未落地。

## 4. 数学建模能力分析

### 4.1 能力矩阵

| 能力项 | 文档层设计/声称 | 当前可运行实现 | 结论 |
|---|---|---|---|
| Excel 读取 | Word 方案列出多份 `.xlsx` 输入和 `openpyxl/pandas` 计划 | 无 `.xlsx` 文件、无读取代码、无依赖清单 | 不支持运行 |
| 数据清洗 | 方案要求检查列名、单位、缺失、重复；论文假设数据已统一 | 无 schema、校验器、清洗流水线或 fixtures | 不支持运行 |
| DEM/GIS | 设计有 30 m DEM、净空、地形剖面、三维视线；计划使用 `rasterio/geopandas` | 无 `DEM.tif/.mat`、CSV/MAT、GIS 代码 | 不支持运行 |
| 可视化 | 计划生成路线、通信状态、甘特图、热力图和 PDF/SVG | 只有封面 PNG/PDF；正文无结果图，且无绘图脚本 | 不支持自动绘图 |
| 优化模型 | Q1 位掩码 DP/二分；Q2 候选列+回溯；Q3 分支定界；Q4 分区枚举/冲突图 | 只有公式、文字和伪代码 | 算法概念存在，执行能力为零 |
| 机器学习 | 未描述 | 无 sklearn、特征、训练/推理代码 | 不支持 |
| 深度学习 | 未描述 | 无 PyTorch/TensorFlow 等 | 不支持 |
| 仿真 | 未描述动态仿真；仅有离散时间/链路采样设计 | 无仿真引擎、场景、随机过程或回放程序 | 不支持 |
| `scipy` | 未见源码或依赖声明 | 无 import/lock 文件 | 无法确认/当前不支持 |
| `pulp` | 仅方案概念提到 MILP 类别 | 无 import/模型文件 | 无法确认/当前不支持 |
| `gurobi` | 未见 | 无 license/config/import | 不支持 |
| `ortools`/CP-SAT | 论文称 Windows 原生 CP-SAT 曾有访问冲突，改用回溯；Word 报告仍将 CP-SAT 列为计划 | 无 OR-Tools 代码或依赖 | 不支持运行 |
| `cvxpy` | 未见 | 无 import/模型文件 | 不支持 |
| LaTeX 论文 | 有完整 `main.tex`、分节源和已编译 `main.pdf` | 可在匹配 TeX 环境手动编译 | 静态能力可用 |
| Word 论文/自动生成 | 有 Word 导出的 PDF 成品 | 无 `.docx` 源和自动生成脚本；无法证明 PDF 由仓库生成 | 仅成品，不自动化 |

### 4.2 数据处理流程（设计 vs 事实）

方案报告列出的预期输入包括：题目 `.docx`、调度中心/服务区 `.xlsx`、物资需求与时限 `.xlsx`、运输无人机 `.xlsx`、中继无人机 `.xlsx`、通信参数 `.xlsx`、`DEM.tif/.mat`、道路/村镇/水体/水系 CSV/MAT 和结果模板 `.xlsx`。这些文件全部不在当前仓库。

设计数据规模（仅来自方案 PDF，不是当前文件）：O01 + S001--S015、80 个不可拆货箱、8 架运输机、2 架中继机、30 m DEM。当前代码无法读取、清洗、投影转换、插值或校验这些数据。

### 4.3 模型与求解流程

- **问题一**：论文 `5_problem1.tex` 给出水平能耗、爬升能耗、返航安全余量和质量/体积约束；在服务区内枚举可行货箱子集，用位掩码 DP 完成覆盖。
- **问题二**：论文 `6_problem2.tex` 定义路线/机型/货箱集合/持续时间/交付偏移量候选列，用逐箱硬时限、无人机占用区间和共享电池充电区间排程；正文说因 CP-SAT 环境问题采用硬时限优先的回溯。
- **问题三**：论文 `7_problem3.tex` 定义双向链路预算、DEM 遮挡、30 s 采样、航段比例 `{0.35, 0.50, 0.65}` 和高度候选，用共享资源分支定界选中继。
- **问题四**：固定问题二/三结构，构造不可拆连接分量和冲突图，枚举 K=2、K=3 分区并核算峰值资源/缺口。

这些是模型说明，不是可导入、可调用或可审计的求解器实现。

### 4.4 测试体系和验证

仓库没有测试目录、测试依赖、夹具、基准答案、CI 或报告生成流水线。唯一的验证语句是附录伪代码中的：

```python
assert verify_all(q1, q2, q3, q4).all_hard_checks_pass
```

`verify_all.py` 不存在，因此无法验证箱子唯一覆盖、容量、SOC、时限、资源区间、通信连续性或分区完整性。敏感性文字声称扫描返航余量、通信容量、DEM 步长和 K 值；Word 方案还计划至少 5 个随机种子，但没有任何运行器、输出、种子或日志。

## 5. 目标赛题适配性：山区洪涝灾害下无人机运输与通信协同优化

### 5.1 总体判断

**设计覆盖率高，工程可执行性为零。** 论文的四问拆解与目标题目高度匹配，但当前 checkout 不能读入题目附件、运行任何一问、输出路径/资源配置或复核论文数字。

### 5.2 四问适配矩阵

| 问题 | 已有的设计能力（[文档]） | 当前缺失能力（[缺失]） | 适配结论 |
|---|---|---|---|
| 问题一：无人机运输能力 | 质量/体积/返航 SOC；DEM 爬升高度和能耗；不可拆货箱候选组批；二分安全载荷和位掩码 DP。论文声称 18 架、57.937430 kWh。 | 原始箱表、机型参数、DEM、物理函数、DP 实现、结果表、单元测试均不存在。 | 模型思路较完整；不能计算或验证。 |
| 问题二：多无人机路径规划/调度 | 异构 A/B/C 机型、多服务区候选列、双服务区路线、逐箱硬时限、8 架实体机、共享电池充电周转；论文声称 20 架、9980 s、61.207790 kWh。 | 路线生成、实体机/电池分配、时间排程器、资源回放、求解日志和路线输出均不存在；论文自述 Q2 结果只是可行解而非全局最优。 | 约束覆盖充分；无路径规划执行器。 |
| 问题三：通信中继优化 | DEM 三维视线、双向 FSPL/链路门限、30 s 轨迹采样、中继位置/高度候选、2 架中继机和能源组件、分支定界；论文声称 14 架直连、6 架需中继、1.797437 kWh。 | DEM/通信参数、视线和链路引擎、中继时序、容量约束和联合调度实现均不存在。主方案假设通信列可并行；严格单实体容量情景文档称有 7 个冲突且不可行。 | 物理问题识别较完整，但结果依赖未给参数的强假设。 |
| 问题四：资源配置 | 固定运输/通信组件、不可拆连接分量、K=2/K=3 分区、峰值并发资源和库存缺口；论文声称缺口分别为 3 和 4。 | 分区枚举、冲突图、资源核算、结果表和约束检查均不存在；不重新排程的口径无法运行验证。 | 分区模型清晰；无资源配置程序。 |

### 5.3 需要特别复核的模型一致性

以下不是由当前仓库可直接裁决的“程序 bug”，而是实施前必须消除的规格歧义：

1. **问题一返程载荷口径**：论文 `5_problem1.tex:5-15` 的返程能耗记为 `E_return(m_p)`；Word 方案的物理口径又写明投送后返程载荷应为 0。两者会影响安全载荷和 18 架次结果。
2. **问题二开始时刻定义**：论文将 `a_j`描述为起飞时刻；Word 方案同时讨论准备/装载开始时刻。硬时限究竟从准备、起飞还是交接结束计时，需要在 schema 和验证器中固定。
3. **问题三离散精度**：论文主文采用 30 s 采样；Word 方案计划用端点、事件点和 10/5/1 s 细网格收敛检查。30 s 采样可能遗漏短时通信中断。
4. **中继容量假设**：主方案允许同一实体中继并行服务不同运输机，严格单实体情景又不可行；中继数量、信道容量和运输开始时刻应联合优化，而不能只在事后加中继。
5. **问题四固定时序**：问题四不重优化问题二/三的时序，只做分区和峰值核算；这与真实“每组独立执行”的重新排程语义可能不同，需要明确是保守核算还是最终部署方案。
6. **结果来源不一致**：Word 方案报告末章说明尚未运行优化程序，而 LaTeX 论文写入具体数值并声称独立回放通过；没有代码和结果快照无法建立来源链。

## 6. 工程优点（限于文档/论文层）

1. **问题分层合理**：Q1→Q4 从运输能力、资源调度、通信耦合到分区配置递进，便于逐层验证和替换求解策略。
2. **共享物理口径意识较强**：论文试图统一 DEM、距离、巡航高度、飞行时间、能耗、SOC 和充电区间，避免四问各自使用不同公式。
3. **候选列/区间思想适合扩展**：将货箱组合与资源占用显式化，比直接对 80 个货箱做全局组合更容易接 MILP/CP-SAT 或定制搜索。
4. **硬约束优先和独立回放意识**：文档强调箱子唯一覆盖、返航余量、时限、无人机/电池/中继能源区间的二次检查，这是后续工程化的正确方向。
5. **局限和敏感性被主动记录**：论文承认 Q2/Q3 非全局最优、通信容量缺参、离散采样、天气/禁飞区未建模；这比无条件宣称最优更可靠。
6. **静态论文构建链完整**：LaTeX 主文件、章节拆分、参考文献、封面资源和编译日志齐全，作为排版制品可追溯到源文件。

## 7. 当前不足

### 7.1 架构和工程问题

- 仓库名义上的 Agent 层完全缺失，不能接收用户题目、规划任务、调用工具或恢复中断。
- 源码、数据、配置、依赖和结果没有分层；生成的 `.aux/.toc/.log/.pdf` 与源文件一起提交，缺少 `src/`、`configs/`、`data/`、`results/` 和版本化 schema。
- 没有安装说明、环境锁定、入口命令、日志约定、错误处理、可重复随机种子或 CI。
- 没有数据 provenance、输入文件哈希、模型参数快照和结果元数据，无法从论文数字追溯到一次运行。
- LaTeX 编译依赖 Windows 字体，但日志记录了字体缺失和 Menlo 替代，跨机器构建不稳定。

### 7.2 Agent 能力缺陷

- 无 Planner/Executor/Memory/Skill/Tool registry/Prompt versioning。
- 无结构化中间状态，Q1 的结果不能由真实程序可靠传递给 Q2，亦无法在 Q2 失败后局部重试。
- 无模型路由或求解器降级策略；论文文字中的 CP-SAT 冲突只能靠手工改为回溯，未形成可配置适配器。
- 无自动实验和评测，无法比较参数、求解时间、最优性 gap、可行率或多随机种子稳定性。

### 7.3 数学/验证风险

- 当前只能看到“可行解”叙述，无法证明最优，也无法检查数值是否由正确的返程载荷、时间起点和离散采样口径产生。
- 通信主方案的并行容量假设是决定性外生假设；在严格容量场景下文档已经报告不可行。
- 30 s 通信采样和有限的中继位置比例候选可能漏掉事件点或更优位置；需要连续/事件驱动复核。
- 未建模降雨、风场、临时禁飞区、需求不确定性和故障恢复，因此不能直接当作灾害现场决策系统。
- 论文没有附路线表、资源甘特图、通信状态时间线、敏感性原始数据或验证报告，审稿/复现实证不足。

## 8. 后续开发建议

### 8.1 短期（1--2 周：建立可运行最小闭环）

1. 恢复并提交题目附件，按 `data/raw`、`data/processed`、`data/schema` 分层；给每个文件记录版本、单位、坐标系和 SHA-256。
2. 建立 Python 包和依赖锁定（`pyproject.toml`/lock 或等价方案），先明确一个可用求解后端（例如 OR-Tools CP-SAT、`scipy.milp` 或明确的 MILP 后端），不要只在文档中列名。
3. 将附录接口落成 `model_utils` + Q1 runner：实现 Excel/DEM 读取、单位校验、公共航段/能耗计算、返航 SOC 和位掩码组批；用最小 fixture 覆盖箱子唯一性、质量/体积、SOC 和 DEM 边界。
4. 实现 `verify_all` 和 JSON/CSV 结果快照；每次运行保存输入哈希、参数、随机种子、求解状态、目标值和约束松弛量。
5. 先做 Q1 端到端验收，再接 Q2；不要在缺数据和验证器时继续扩写“已完成”的数值结论。
6. 将 LaTeX 构建与模型运行分离；把生成 PDF/日志放进可重建目录或 CI artifact，固定字体/容器，修复当前字体警告。

### 8.2 中期（1--2 个月：完成四问求解与实验闭环）

1. 实现 Q2 候选列、实体机/电池区间和硬时限求解，给出可证明的可行性/最优性状态、求解时间和 gap；对回溯算法写完备性说明或改用标准求解器。
2. 实现 Q3 事件驱动或细网格的 DEM 三维视线与双向链路引擎，把中继数量、信道容量、服务区间和运输起飞时刻放入联合优化。
3. 将 Q4 从“固定时序核算”扩展为可选的组内重排，并同时报告最小并发配置与保守实体 ID 配置，明确两种口径。
4. 建立 `experiments/` 配置矩阵：返航余量、能耗系数、净空、链路裕量、采样步长、通信容量、随机种子；自动产出表格、图、日志和可追溯 Markdown/LaTeX 片段。
5. 加入 pytest/属性测试、回归 fixture、求解器不可用时的降级测试和 CI；将论文中的数值由结果文件自动回填，禁止手工复制。

### 8.3 长期：形成数学建模 Agent 平台

```text
用户题目/附件
  -> Planner（题目解析、子问题 DAG、预算和验收标准）
  -> Skill Router（数据、DEM、优化、通信、绘图、论文技能）
  -> Executor（可恢复任务、工具调用、重试、并行实验）
  -> Solver/Tool Adapters（CP-SAT/MILP/数值/地理/绘图）
  -> Memory/Artifact Store（schema、运行状态、结果、证据和版本）
  -> Verifier（硬约束、单位、最优性和物理一致性）
  -> Report Composer（表格、图、LaTeX/Word、审计摘要）
```

平台化时应增加：模型/求解器 provider 抽象、Skill manifest 与版本兼容性、结构化事件日志、人工确认点、成本/超时预算、数据脱敏、实验数据库、评测集和多题目插件。天气与需求不确定性可在稳定确定性基线后加入鲁棒/两阶段优化；机器学习或深度学习只有在存在预测数据和明确决策接口时再引入，不应为了“Agent 化”而虚增模块。

## 9. 最终判定

| 维度 | 当前等级 | 判定 |
|---|---:|---|
| 项目结构可读性 | 3/5 | 文件少、论文分节清晰，但没有软件工程目录。 |
| 数学建模设计 | 3/5 | 四问约束覆盖较全，仍有规格歧义。 |
| 可执行求解能力 | 0/5 | 无源码、数据、依赖和入口。 |
| Agent 能力 | 0/5 | Planner/Executor/Memory/Skill/Tool calling 均不存在。 |
| 自动实验与测试 | 0/5 | 只有伪代码 `assert` 和文字计划。 |
| 论文构建 | 2/5 | LaTeX 源和历史 PDF 可用，但环境依赖和自动回填缺失。 |
| 结果可复现性 | 0/5 | 论文数值无法由当前 checkout 追溯或重算。 |

**一句话画像：这是一个已经写出建模叙事和论文成品的阶段性仓库，不是当前可运行、可调用、可验证的数学建模 Agent。**

